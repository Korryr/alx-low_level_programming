#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
/**
 * main - a program that prints letters in lower case
 *
 * Return: 0 always (success)
 */
int main(void)
{
	char  letter;
	for (letter = 'a'; letter <= 'z'; letter++'\n')

	putchar(letter);

	putchar('\n');
		return (0);
}
