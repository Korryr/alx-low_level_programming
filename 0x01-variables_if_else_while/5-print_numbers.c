#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
/**
 * main - A program that writes numbers from 0 to 10
 *
 * Return: 0 always (success>
 */

int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
		printf("%d", num);
	return (0);
}
